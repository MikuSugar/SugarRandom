package me.mikusugar.random.core.service;

import me.mikusugar.random.core.bean.AliasConfig;
import org.springframework.data.jpa.repository.JpaRepository;

/**
 * @author mikusugar
 */
public interface AliasConfigRepository extends JpaRepository<AliasConfig,String> { }
